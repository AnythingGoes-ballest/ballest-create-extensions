[meta]
name = "Create Extensions"
version = "0.3.0"
author = "AnythingGoes"
description = "Track editor extras: groups (G / U), rotate modes in the toolbar (center, mirrored), Shift/Ctrl+click to deselect, whole-unit placement, placement distance, rotated copies, Tab between transform boxes."
min_host = "0.10.0"

[script]
files = ["main.as"]
assets = ["keyboard_g.png", "keyboard_u.png"]
