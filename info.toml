[meta]
name = "Create Extensions"
version = "0.1.0"
author = "AnythingGoes"
description = "Track editor extras: whole-unit placement, placement distance, rotated copies, Tab between transform boxes."
min_host = "0.6.0"

[script]
files = ["main.as"]
timeout = 30
